# 只設寬度，與 sora-to-ki 對齊（讀兩包 repo 是同一把尺）。
# 實測這份 code 的行長 p99 = 109、超過 110 字元的只有 103 行 —— 天然就落在 110 以內。
#
# ⚠️ 刻意**不設** `use_small_heuristics = "Max"`。sora-to-ki 有那條，是為了保住它
# 刻意寫緊湊的 struct literal；這包的 code 寫得比較展開，加上去反而多 10 個重排區塊
# （實測 590 → 600）。那邊的數值是為了「最小化重排」反推的，前提在這裡不成立。
max_width = 110
