# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
# ⚠️ 不要寫成 `/node_modules` —— 開頭的斜線把它釘在 repo 根，子目錄的
#    node_modules（例如誤在 backend/ 執行 pnpm add 產生的）就**不會**被忽略，
#    然後被 commit 進去。這裡實際發生過一次。
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# typescript
*.tsbuildinfo

# Rust（workspace root 在此，target/ 與 Cargo.lock 都落在根目錄）
/target

# monorepo 產物
/packages/*/dist

# Vite
/dist
.tanstack

# GlitchTip source map 上傳用的 API token（compose 的 build secret 來源）。
# ⚠️ 檔案本身必須存在，compose 才不會報錯；空檔會走「跳過上傳」分支。
/sentry-token
